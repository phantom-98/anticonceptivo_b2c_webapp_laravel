import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <h2 className="font-signika font-35 bold text-primary mb-0" style={{ marginLeft : '-2px'}}>{title}</h2>
    );
};

export default SectionTitle;

